services:
  - type: web
    name: mighty-truck-adventure
    env: node
    buildCommand: npm install
    startCommand: npm run render-start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0 